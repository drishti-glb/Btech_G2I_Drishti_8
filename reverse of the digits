#include <stdio.h>
int main()
{
    int rvrs=0,n,dig;
    
    printf("enter the number:");
    scanf("%d",&n);
    
   while(n>0)
   {
       dig=n%10;
       rvrs=(rvrs*10)+dig;
       n=n/10;
   }
        printf("The reverse of digits is:%d",rvrs);
    
    return 0;
}
