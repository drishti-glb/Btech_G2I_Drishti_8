#include <stdio.h>
int main()
{
    int sum=0,n,dig;
    
    printf("enter the number:");
    scanf("%d",&n);
    
   while(n>0)
   {
       dig=n%10;
       sum=sum+dig;
       n=n/10;
   }
        printf("The sum of digits is:%d",sum);
    
    return 0;
}
